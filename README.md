# TODOS

- [ ] fill in ClassificationRequest and ExtractionRequest pydantic models
- [ ] define and implement use of ClassifyResponse and ExtractionRespoinse pydantic models
- [ ] get code that passes request to classify and extract functions to call ollama api and get back answer working with api endpoints
- [ ] refactor and make code more readable 
- [ ] do i need the json schemas anymore? thinking no